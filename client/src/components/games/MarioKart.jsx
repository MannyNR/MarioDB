const MarioKart64 = (props) => {
  const { characters } = props;
  return (
    <div className="mario-kart64">
      <h1>{games.name}</h1>
      <h2>{games.characters}</h2>

    </div>
  )
}
export default MarioKart64